<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/backGroundColor</item>
        <item name="colorPrimaryDark">@color/backGroundColor</item>
        <item name="colorAccent">@color/greenNeon</item>
        <item name="android:windowBackground">@color/backGroundColor</item>
        <item name="android:colorBackground">@color/backGroundColor</item>
        <item name="android:statusBarColor">#a52e286c</item>
        <item name="android:textColorSecondary">@color/greenNeon</item>
        <item name="android:textColorPrimary">#ffffff</item>
        <item name="android:buttonStyle">@style/ButtonColor</item>
        <item name="android:textColorHint">#c69b9999</item>

    </style>

    <style name="ButtonColor">
        <item name="android:textColor">#0e1e43</item>
    </style>
</resources>
